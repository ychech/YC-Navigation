# ============================================
# 艺术导航 - 环境变量配置示例
# 复制此文件为 .env 并修改配置
# ============================================

# ============================================
# 数据库配置 (重要!)
# ============================================

# 数据库类型: sqlite 或 mysql
# - sqlite: 轻量级，单文件存储，适合 2C2G (推荐)
# - mysql: 高性能，需要更多内存 (~700MB)
DB_PROVIDER=sqlite

# SQLite 配置 (DB_PROVIDER=sqlite 时使用)
# 数据库文件将存储在 ./data/prod.db
DATABASE_URL="file:./data/prod.db"

# MySQL 配置 (DB_PROVIDER=mysql 时使用)
# 使用 Docker 内部网络时，主机名为 mysql
# DATABASE_URL="mysql://navuser:your_password@mysql:3306/artistic_nav"

# MySQL Root 密码 (仅在启用 MySQL 服务时需要)
# MYSQL_ROOT_PASSWORD=change_me_root_123
# MYSQL_USER_PASSWORD=change_me_user_123

# ============================================
# Next.js / NextAuth 配置
# ============================================

# 随机密钥，用于加密 (可使用: openssl rand -base64 32 生成)
NEXTAUTH_SECRET=your_random_secret_key_here

# 应用访问地址
# - 使用IP: http://39.102.80.128
# - 使用域名: https://your-domain.com
NEXTAUTH_URL=http://39.102.80.128

# ============================================
# 管理员配置
# ============================================

# 后台登录密码
ADMIN_PASSWORD=your_admin_password

# ============================================
# 存储配置
# ============================================

# 存储类型: local 或 oss
# - local: 文件存储在服务器本地 ./uploads 目录
# - oss: 阿里云对象存储
STORAGE_TYPE=local

# 本地存储路径 (STORAGE_TYPE=local 时使用)
UPLOAD_DIR=./uploads

# OSS 配置 (STORAGE_TYPE=oss 时使用)
# OSS_REGION=oss-cn-beijing
# OSS_BUCKET=your-bucket-name
# OSS_ACCESS_KEY_ID=your-access-key-id
# OSS_ACCESS_KEY_SECRET=your-access-key-secret
# OSS_ENDPOINT=https://${OSS_REGION}.aliyuncs.com
# OSS_CDN_DOMAIN=https://your-cdn-domain.com (可选)

# ============================================
# 其他配置
# ============================================

# 应用端口 (Docker 内部使用，一般不需要修改)
PORT=3000

# 禁用 Next.js 遥测
NEXT_TELEMETRY_DISABLED=1
