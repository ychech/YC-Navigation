# ==========================================
# 数据库配置 (Database Configuration)
# 支持 MySQL 和 SQLite 两种模式
# ==========================================

# --- SQLite 模式 (推荐用于 2C2G 小内存服务器) ---
# 优点：零配置、单文件、内存占用极低
# 注意：路径相对于 prisma/schema.prisma 文件位置
DB_PROVIDER=sqlite
DATABASE_URL="file:./dev.db"

# --- MySQL 模式 (适合高并发、多实例部署) ---
# DB_PROVIDER=mysql
# DATABASE_URL="mysql://root:123456@127.0.0.1:3306/artistic_nav"

# ==========================================
# 管理员密码 (Admin Password)
# ==========================================
ADMIN_PASSWORD=admin123

# ==========================================
# 存储配置 (Storage Configuration)
# Options: 'local' | 'oss'
# ==========================================
STORAGE_TYPE=local

# 阿里云 OSS 配置 (仅在 STORAGE_TYPE=oss 时需要)
# OSS_REGION=oss-cn-hangzhou
# OSS_ACCESS_KEY_ID=your-access-key
# OSS_ACCESS_KEY_SECRET=your-secret-key
# OSS_BUCKET=your-bucket
# OSS_DOMAIN=https://your-domain.com
